# {{Initiative}} — execution progress

## Execution state

**Initiative status:** not started  
**Current task:** none  
**Next task:** `task-01-{{slug}}`  
**Design authority:** `.agent-artifacts/{{initiative-slug}}/design.md`

Use only `not started`, `in progress`, `blocked`, or `complete` for initiative status. `Next task` names one runnable task whose prerequisites are complete, or `none` when no task is runnable.

## Task checklist

- [ ] `pending` — `task-01-{{slug}}`

## Completed outputs and validation

None — execution has not started.

## Discoveries and deviations

None.

## Blockers

None known. For each blocker, record the affected task, concrete evidence, what would unblock it, and whether independent tasks remain runnable.

## Next action

Run `task-01-{{slug}}` in a fresh iteration.
